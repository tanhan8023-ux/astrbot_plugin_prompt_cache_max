{
  "enabled": {
    "description": "总开关。关闭后插件不参与缓存观察或注入。",
    "type": "bool",
    "default": true
  },
  "observe_requests_enabled": {
    "description": "观察大模型请求并记录轻量检查信息。默认关闭，避免影响正常回复。",
    "type": "bool",
    "default": false
  },
  "provider_wrapping_enabled": {
    "description": "包装模型提供商方法，用于读取或修改实际发出的请求。默认关闭，避免中转站因额外字段卡住。",
    "type": "bool",
    "default": false
  },
  "cache_injection_enabled": {
    "description": "注入缓存字段（如 prompt_cache_key）。需要同时开启 provider_wrapping_enabled。默认关闭，更安全。",
    "type": "bool",
    "default": false
  },
  "providers": {
    "description": "允许启用缓存能力的模型提供商类型。",
    "type": "list",
    "default": ["openai", "anthropic", "gemini"]
  },
  "allowlist_base_urls": {
    "description": "接口地址白名单。只有这些地址会被允许注入专用缓存字段。",
    "type": "list",
    "default": [
      "https://api.openai.com",
      "https://api.anthropic.com",
      "https://generativelanguage.googleapis.com",
      "https://aiplatform.googleapis.com",
      "https://aiwork.fans",
      "https://aiwork.fans/v1",
      "https://api.55api.com",
      "https://api.55api.com/v1",
      "https://api.55api.cn",
      "https://api.55api.cn/v1",
      "https://api.55.ai",
      "https://api.55.ai/v1",
      "https://api.55.al",
      "https://api.55.al/v1",
      "https://tokens.ai-tokens.app",
      "https://tokens.ai-tokens.app/v1"
    ]
  },
  "cache_ttl": {
    "description": "不同提供商的缓存保留时间配置。",
    "type": "object",
    "items": {
      "anthropic": {
        "description": "Claude 临时缓存保留时间。",
        "type": "string",
        "default": "5m"
      },
      "gemini": {
        "description": "Gemini 显式缓存保留时间。",
        "type": "string",
        "default": "3600s"
      }
    },
    "default": {
      "anthropic": "5m",
      "gemini": "3600s"
    }
  },
  "openai_compatible_hosts": {
    "description": "按 OpenAI 兼容接口处理的域名。即使模型名包含 claude/gemini，也会走 OpenAI 兼容缓存字段。",
    "type": "list",
    "default": [
      "aiwork.fans",
      "api.55.al",
      "api.55.ai",
      "api.55api.com",
      "api.55api.cn",
      "tokens.ai-tokens.app"
    ]
  },
  "openai_prompt_cache_retention": {
    "description": "可选的 OpenAI 缓存保留字段。未知中转站建议保持关闭。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "是否发送 prompt_cache_retention 字段。",
        "type": "bool",
        "default": false
      },
      "value": {
        "description": "缓存保留时间取值。",
        "type": "string",
        "default": "24h"
      }
    },
    "default": {
      "enabled": false,
      "value": "24h"
    }
  },
  "min_prefix_tokens": {
    "description": "稳定前缀达到多少估算 token 后，才尝试发送缓存提示。",
    "type": "object",
    "items": {
      "openai": {
        "description": "发送 OpenAI 兼容缓存键（prompt_cache_key）的最低前缀长度。",
        "type": "int",
        "default": 512
      },
      "gemini_flash": {
        "description": "尝试 Gemini Flash 显式缓存的最低前缀长度。",
        "type": "int",
        "default": 1024
      },
      "gemini_pro": {
        "description": "尝试 Gemini Pro 显式缓存的最低前缀长度。",
        "type": "int",
        "default": 4096
      },
      "anthropic": {
        "description": "添加 Claude 缓存标记（cache_control）的最低前缀长度。",
        "type": "int",
        "default": 512
      }
    },
    "default": {
      "openai": 512,
      "gemini_flash": 1024,
      "gemini_pro": 4096,
      "anthropic": 512
    }
  },
  "threshold_slack_tokens": {
    "description": "估算 token 的安全余量。当前缀只差一点达到门槛时，也允许发送缓存提示。",
    "type": "object",
    "items": {
      "openai": {
        "description": "OpenAI 兼容接口的门槛余量。",
        "type": "int",
        "default": 16
      },
      "anthropic": {
        "description": "Claude 的门槛余量。",
        "type": "int",
        "default": 16
      },
      "gemini": {
        "description": "Gemini 的门槛余量。",
        "type": "int",
        "default": 128
      }
    },
    "default": {
      "openai": 16,
      "anthropic": 16,
      "gemini": 128
    }
  },
  "stats_enabled": {
    "description": "保存轻量缓存统计信息。",
    "type": "bool",
    "default": true
  },
  "max_claude_cache_blocks": {
    "description": "最多给 Claude 请求写入多少个缓存标记点。",
    "type": "int",
    "default": 4
  },
  "stable_style_rules": {
    "description": "在系统提示词前加入稳定风格规则。可增加稳定前缀长度，但默认关闭，避免影响人设。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "是否启用稳定风格规则。",
        "type": "bool",
        "default": false
      },
      "prepend_to_system_prompt": {
        "description": "是否把风格规则插到 system_prompt 最前面。",
        "type": "bool",
        "default": true
      },
      "mode": {
        "description": "内置风格模式。",
        "type": "string",
        "default": "warm_soft_sarcasm"
      },
      "text": {
        "description": "自定义稳定风格文本。留空时使用内置温柔轻微毒舌风格。",
        "type": "string",
        "default": ""
      }
    },
    "default": {
      "enabled": false,
      "prepend_to_system_prompt": true,
      "mode": "warm_soft_sarcasm",
      "text": ""
    }
  },
  "exact_response_cache": {
    "description": "精确回复缓存。v0.5.1 起为保持自然回复会被插件强制关闭；真正省钱请看服务端 cached_tokens。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "是否启用精确回复缓存。当前版本会强制关闭，避免 bot 复读旧答案。",
        "type": "bool",
        "default": false
      },
      "backend": {
        "description": "缓存后端，可选 memory 或 redis。",
        "type": "string",
        "default": "memory"
      },
      "redis_url": {
        "description": "当后端为 redis 时使用的 Redis 地址。",
        "type": "string",
        "default": "redis://localhost:6379/0"
      },
      "ttl_seconds": {
        "description": "精确回复缓存保留秒数。",
        "type": "int",
        "default": 600
      },
      "max_prompt_chars": {
        "description": "超过这个序列化长度的 prompt 不进入精确回复缓存。",
        "type": "int",
        "default": 12000
      }
    },
    "default": {
      "enabled": false,
      "backend": "memory",
      "redis_url": "redis://localhost:6379/0",
      "ttl_seconds": 600,
      "max_prompt_chars": 12000
    }
  }
}
